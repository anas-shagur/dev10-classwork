# exercise_02

1. Open `package.chess` and find the `Queen` class.
2. Open `tests.test_chess` and find the `TestQueen` class.
2. To understand `Queen`, read its docstring.
3. Implement the numbered changes in each class. 

    For each change, drive your development by writing tests.

    In `/week-02/exercises/05-stateful-unit-tests`, execute this command:

    ```sh
    05-stateful-unit-tests $ python -m unittest tests.test_chess
    ```