# exercise_01

1. Open `package.ocean` and find the `Submarine` class.
2. Open `tests.test_ocean` and find the `TestSubmarine` class.
3. To understand `Submarine`, read its docstring.
4. Implement the numbered changes in each class. For each change, drive your development by writing tests.

    In `/week-02/exercises/05-stateful-unit-tests`, execute this command:

    ```sh
    05-stateful-unit-tests $ python -m unittest tests.test_ocean
    ```

    Your process should be:
    - Write some code. Confirm it's correct with a test.
    - Write some code. Confirm it's correct with a test.
    - Repeat until complete.

    