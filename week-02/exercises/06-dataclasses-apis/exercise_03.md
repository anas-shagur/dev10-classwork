# Add Data Classes

1. Create a module named `insects.py`.

2. Create a data class named `Insect` with appropriate attributes (latin name and common name).

3. Create a data class named `Order` with appropriate attributes (latin name, common name, and a list of `Insect`s).