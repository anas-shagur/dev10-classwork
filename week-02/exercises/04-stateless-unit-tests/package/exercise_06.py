#  1. Read the capitalize_all docstring.
#  2. Implement capitalize_all.
#  3. Implement suggestions in TestExercise06.
#  4. Confirm implementation correctness by running tests.
#  5. Stretch Goal: instead of capitalizing the first character of each element, capitalize the first character
#  of each word in each element.


class Exercise06:
    def capitalize_all(self, values):
        """
        Capitalizes the first character of each element in a list[str]
        and returns the result in a new list.
        A None argument should return None.
        An empty list should return a new empty list.
        None or empty array elements should be ignored.

        Args:
            values (list[str]): a list containing elements to capitalize.
        Returns:
            a new list with each element capitalized.

        """
        return None
