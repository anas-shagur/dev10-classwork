#  1. Read the reverse docstring.
#  2. Implement reverse.
#  3. Create tests for reverse and confirm that it is correct.


class Exercise07:
    def reverse(self, values):
        """
        Reverses the order of elements in a list[str] argument and returns them in a new list.
        It does not alter the existing list.

        Args:
            values (list[str]): the list to reverse
        Returns:
            a new list with elements in reverse order.
        """
        return None
