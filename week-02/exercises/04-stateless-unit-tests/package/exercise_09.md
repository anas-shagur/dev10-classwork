# Exercise09

1. Add a module named `list_functions`.
2. Add a function:

    ```
    Name: remove_zero
    Inputs: list[int]
    Output: list[int]
    Description: Finds all non-zero elements and copy them to a new list.
    Use a list comprehension.
    Stretch goal: confirm the list is all integers. 
    ```

3. Create tests for remove_zero and confirm it is correct.