# exercise_13

1. Edit `hero.py` and add a class named `Hero`. It represents a super hero with powers.

2. Add an initializer:
    - name should be a string, the super hero name
    - powers is a list[Power], a hero can have multiple powers

3. Add a `@property` for both the name and powers fields.