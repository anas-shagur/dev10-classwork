# exercise_06

1. Add a Python script, `ballon.py`, and add a class named `Balloon`.

2. Add an initializer:
    - color should be a string
    - psi should be a float with a default value, `0.0`

3. Add `@property` for both color and psi (psi will always have its default value 0.0).