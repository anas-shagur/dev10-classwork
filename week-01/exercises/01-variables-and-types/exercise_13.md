# Exercise 13

1. Add a new Python script named `exercise_13.py` to this directory.
3. Declare a variable `grade_level` and assign a literal value 1-12. Determine the appropriate type.
4. Declare a boolean variable `is_senior`. Use an expression to set the value to `True` if `grade_level` equals `12`.
5. Declare a boolean variable `is_interested_in_volunteering`.
6. Declare a boolean variable `should_send_volunteer_info`. Use an expression to set the value. It should be `True` if both `is_senior` and `is_interested_in_volunteering` are `True`.
7. Print the final result.