import random

numbers = set()

# 1. Generate random numbers between 0 and 9.
# 2. Use a `while` loop and a condition `len(numbers) < 10`
# to add a random number to the set and print its results.
# 3. Then print the set _after_ the while loop.
# It should result in 10 unique numbers.
