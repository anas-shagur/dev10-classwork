# 1. Change the code to accept input from the user.
# Use the `input` function and parse the value with `int()`.
value = int(input("Enter a number: "))
if value >= 10 and value < 26:
    print("Value is in the acceptable range.")
